services:
  mongo:
    image: mongo:7
    container_name: alumni-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    volumes:
      - alumni_mongo_data:/data/db

volumes:
  alumni_mongo_data:
